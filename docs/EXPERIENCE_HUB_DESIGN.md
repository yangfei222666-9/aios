# Experience Hub Design - 经验联邦协议设计

**版本：** v0.2 (Design-Only)  
**状态：** design-only / draft / review-pending  
**定位：** P2/P3 级生态方向，非当前主线  
**阶段：** Phase 0 - 手动经验包  
**实施状态：** not started

---

## 1. 目标与边界

### 1.1 这是什么

Experience Hub 是一个 **经验回传与聚合体系**，用于：

- 从多个太极OS实例收集成功经验、规则候选、使用统计
- 在中心进行验证、脱敏、提炼
- 生成可分发的改进建议
- 让其他实例可选地应用这些改进

**核心价值：** 让太极OS的进化不只依赖单个实例的经验，而是能从整个生态中学习。

### 1.2 这不是什么

**不是狭义的联邦学习：**
- 不聚合模型梯度
- 不做本地训练参数同步
- 不做分布式模型训练

**不是自动更新系统：**
- 不自动推送代码更新
- 不自动修改本地配置
- 不自动应用改进

**不是数据收集平台：**
- 不收集原始日志
- 不收集用户隐私数据
- 不收集未经脱敏的执行记录

### 1.3 为什么需要它

**当前问题：**
1. 每个太极OS实例都在独立学习，重复踩坑
2. 成功经验无法跨实例复用
3. 规则改进只能靠单个实例的有限样本
4. 新用户需要从零开始积累经验

**期望效果：**
1. 成功经验可以跨实例共享
2. 规则改进基于更大样本
3. 新用户可以快速获得经过验证的规则
4. 生态整体进化速度加快

### 1.4 当前阶段定位

**Phase 0: 手动经验包**

只定义协议和流程，不做自动化实现。

**包含：**
- 本地经验包格式
- 脱敏规则
- 中心接收与验证规则
- 人工审核流程

**不包含：**
- 自动回传
- 自动聚合
- 自动分发
- 自动应用更新

**原因：** 这个方向一次性碰到 4 个高复杂度问题（隐私与脱敏、协议与版本兼容、中心验证与反污染、更新分发与回滚），需要先把协议设计清楚，再考虑实现。

---

## 2. 术语定义

### 2.1 核心概念

**经验（Experience）**
- 定义：一次成功的任务执行记录，包含触发条件、执行过程、结果验证
- 格式：结构化数据（JSON）
- 来源：本地 Agent 执行记录、任务队列、lesson 系统

**Lesson**
- 定义：从失败或成功中提炼的可复用知识
- 格式：结构化数据（JSON）+ 自然语言描述
- 来源：lesson 系统、人工标注

**Rule**
- 定义：经过验证的可执行规则，可直接应用到系统中
- 格式：代码片段 + 触发条件 + 验证方式
- 来源：从 lesson 提炼、从经验聚合

**Improvement**
- 定义：对现有 Agent/Skill/配置的改进建议
- 格式：diff + 说明 + 验证方式
- 来源：从规则聚合、从经验分析

**经验包（Experience Package）**
- 定义：本地生成的、经过脱敏的经验数据集合
- 格式：压缩的 JSON 文件
- 内容：经验 + lesson + 统计数据（不含原始日志）

**经验中心（Experience Hub）**
- 定义：接收、验证、提炼、分发经验的中心服务
- 职责：验证格式、安全扫描、人工审核、生成改进建议

### 2.2 数据层级

```
原始执行记录（本地，不上传）
    ↓ 脱敏 + 提炼
经验包（可选上传）
    ↓ 验证 + 聚合
规则候选（中心）
    ↓ 人工审核
改进建议（可分发）
    ↓ 用户确认
应用到本地（可选）
```

---

## 3. 本地经验包 Schema

### 3.1 格式定义

**文件格式：** JSON  
**压缩格式：** gzip  
**文件名：** `experience-{instance_id}-{timestamp}.json.gz`

### 3.2 顶层结构（Phase 0 最小集）

```json
{
  "version": "0.2.0",
  "instance_id": "uuid",
  "generated_at": "2026-03-11T17:51:00+08:00",
  "taijios_version": "0.3.0",
  "privacy_level": "stats-only",
  "period": {
    "start": "2026-03-01T00:00:00+08:00",
    "end": "2026-03-11T17:51:00+08:00"
  },
  "stats": {...},
  "lessons_summary": [...],
  "rules_summary": [...]
}
```

**Phase 0 不包含：**
- `experiences` - 原始经验列表（Phase 1+）
- `metadata` - 详细元数据（Phase 1+）
- 任何原始日志或执行记录

### 3.3 字段说明

**必需字段（Phase 0）：**
- `version` - 经验包格式版本（语义化版本）
- `instance_id` - 实例唯一标识（UUID，匿名化）
- `generated_at` - 生成时间（ISO 8601）
- `taijios_version` - 太极OS 版本
- `privacy_level` - 隐私级别（固定 3 档，见 4.2）
- `period` - 统计时间范围
- `stats` - 统计数据（必需）

**可选字段（Phase 0）：**
- `lessons_summary` - lesson 候选摘要（不含原始上下文）
- `rules_summary` - rule 候选摘要（不含原始代码）

### 3.4 Stats Schema（Phase 0 核心）

```json
{
  "agents": {
    "total": 30,
    "active": 15,
    "success_rate": 0.85
  },
  "tasks": {
    "total": 1000,
    "completed": 850,
    "failed": 150
  },
  "skills": {
    "total": 50,
    "used": 30,
    "success_rate": 0.90
  }
}
```

**只包含聚合统计，不包含：**
- 单次执行记录
- 原始日志
- 错误详情
- 文件路径
- 用户数据

### 3.5 Lessons Summary Schema（可选）

```json
{
  "id": "uuid",
  "type": "failure_pattern",
  "agent": "Coder",
  "pattern": "timeout_on_large_file",
  "frequency": 5,
  "confidence": 0.85,
  "suggested_fix": "increase_timeout_or_chunk"
}
```

**禁止包含：**
- 原始错误信息
- 文件路径
- 代码片段
- 用户输入

### 3.6 Rules Summary Schema（可选）

```json
{
  "id": "uuid",
  "type": "optimization",
  "target": "GitHub_Researcher",
  "rule": "increase_timeout_threshold",
  "confidence": 0.90,
  "sample_count": 10
}
```

**禁止包含：**
- 完整代码
- 配置文件
- 执行命令

---

## 4. 脱敏与用户控制

### 4.1 硬规则 1：默认不自动上传，必须 opt-in

**实现方式：**
- 配置文件中 `experience_hub.enabled` 默认为 `false`
- 用户必须显式设置为 `true` 才能启用
- 首次启用时显示隐私声明和数据范围

**配置示例：**
```json
{
  "experience_hub": {
    "enabled": false,
    "privacy_level": "stats-only",
    "upload_interval": "weekly",
    "auto_apply_improvements": false
  }
}
```

### 4.2 硬规则 2：默认只传 stats-only，不传原始日志

**隐私级别定义（Phase 0 固定 3 档）：**

| 级别 | 包含内容 | 不包含内容 |
|------|---------|-----------|
| `stats-only` | 聚合统计数据、成功率、计数 | 原始日志、输入输出、文件路径、lesson/rule 详情 |
| `summary-only` | stats + lesson/rule 候选摘要 | 原始执行记录、代码片段、错误详情 |
| `review-required` | stats + summaries + 需人工审核的详细信息 | 原始日志、用户数据（需额外审核） |

**默认级别：** `stats-only`

**Phase 0 不支持：**
- `experiences` 级别（原始经验记录）
- `full` 级别（所有数据）

### 4.3 脱敏规则

**自动脱敏：**
1. 移除所有文件路径（替换为 `<path>`）
2. 移除所有 URL（替换为 `<url>`）
3. 移除所有 API 密钥（替换为 `<key>`）
4. 移除所有邮箱地址（替换为 `<email>`）
5. 移除所有 IP 地址（替换为 `<ip>`）
6. 移除所有用户名（替换为 `<user>`）
7. 移除所有时间戳中的精确时间（只保留日期）

**保留内容：**
- Agent 名称
- Skill 名称
- 任务类型
- 成功/失败状态
- 执行时长（范围）
- 错误类型（不含具体错误信息）

**用户可控：**
- 用户可以在上传前预览经验包内容
- 用户可以手动排除特定 Agent/Skill
- 用户可以设置额外的脱敏规则

### 4.4 本地验证

**上传前检查：**
1. 格式验证（JSON schema）
2. 脱敏验证（扫描敏感信息）
3. 大小限制（单个经验包 < 10MB）
4. 内容审查（禁止包含原始日志）

**失败处理：**
- 验证失败 → 不上传，记录错误
- 用户可以查看验证报告
- 用户可以手动修正后重试

---

## 5. 中心验证与提炼流程

### 5.1 接收与格式验证

**步骤 1：接收**
- 接收经验包（HTTP POST）
- 验证文件大小（< 10MB）
- 验证压缩格式（gzip）
- 生成接收 ID

**步骤 2：格式验证**
- 解压缩
- JSON schema 验证
- 版本兼容性检查
- 必需字段检查

**失败处理：**
- 格式错误 → 拒绝，返回错误信息
- 版本不兼容 → 拒绝，提示升级
- 字段缺失 → 拒绝，返回缺失字段列表

### 5.2 安全扫描（反污染）

**扫描项：**
1. 恶意代码检测（代码注入、命令注入）
2. 数据污染检测（异常统计、虚假数据）
3. 隐私泄露检测（残留敏感信息）
4. 格式攻击检测（超大字段、嵌套炸弹）

**检测方式：**
- 静态规则扫描
- 异常值检测
- 黑名单匹配
- 人工抽查

**失败处理：**
- 检测到恶意内容 → 拒绝 + 标记实例
- 检测到隐私泄露 → 拒绝 + 通知用户
- 检测到数据污染 → 拒绝 + 记录日志

### 5.2.1 中心侧拒收规则（硬约束）

**必须拒收的内容：**

1. **含密钥/令牌**
   - API 密钥
   - 访问令牌
   - 密码
   - 证书

2. **含路径/账号/主机名**
   - 文件系统路径
   - 用户名
   - 主机名
   - IP 地址
   - 邮箱地址

3. **含原始长日志**
   - 单条日志 > 1KB
   - 日志总量 > 100 条
   - 包含堆栈跟踪
   - 包含完整错误信息

4. **含不可解释的可执行内容**
   - 二进制数据
   - Base64 编码的代码
   - 混淆的脚本
   - 未签名的可执行文件

5. **含来源不明的规则包**
   - 无 instance_id
   - 无版本号
   - 无时间戳
   - 格式不符合 schema

**拒收响应格式：**
```json
{
  "status": "rejected",
  "reason": "contains_api_key",
  "details": "Found API key pattern in field: lessons_summary[2].context",
  "timestamp": "2026-03-11T17:51:00+08:00"
}
```

### 5.3 人工审核

**审核流程：**
1. 自动扫描通过 → 进入审核队列
2. 审核员查看经验包内容
3. 判断是否有价值
4. 判断是否可以提炼为规则
5. 标记为：通过 / 拒绝 / 需要更多信息

**审核标准：**
- 数据真实性
- 经验可复用性
- 规则通用性
- 安全性

**审核结果：**
- 通过 → 进入提炼流程
- 拒绝 → 记录原因，不进入提炼
- 需要更多信息 → 联系用户（可选）

### 5.4 提炼为改进建议

**提炼流程：**
1. 聚合多个实例的相似经验
2. 识别共性模式
3. 生成规则候选
4. 验证规则有效性
5. 生成改进建议

**改进建议格式：**
```json
{
  "id": "uuid",
  "type": "rule_improvement",
  "target": "agent/GitHub_Researcher",
  "title": "优化超时阈值",
  "description": "基于 50 个实例的数据，建议将超时阈值从 60s 提高到 120s",
  "confidence": 0.90,
  "sample_count": 50,
  "diff": "...",
  "validation": "...",
  "created_at": "2026-03-11T17:51:00+08:00"
}
```

### 5.5 生成可分发包

**分发包格式（Phase 0 最小集）：**
```json
{
  "package_id": "uuid",
  "source_batch_id": "uuid",
  "version": "0.2.0",
  "type": "improvement_bundle",
  "generated_at": "2026-03-11T17:51:00+08:00",
  "compatibility_version": "0.3.0",
  "improvements": [...],
  "signature": "...",
  "checksum": "...",
  "rollback_id": "uuid"
}
```

**必需字段：**
- `package_id` - 分发包唯一标识
- `source_batch_id` - 来源批次标识（可追溯到哪批经验包）
- `generated_at` - 生成时间
- `signature` - Ed25519 签名
- `rollback_id` - 回滚点标识
- `compatibility_version` - 兼容的太极OS版本

**签名与校验：**
- 使用 Ed25519 签名
- 包含完整性校验（SHA-256）
- 可追溯到审核员（匿名 ID）

---

## 6. 分发原则与禁止项

### 6.1 硬规则 3：任何改进默认不自动应用

**实现方式：**
- 改进建议下载到本地后，默认状态为 `pending`
- 用户必须显式确认才能应用
- 应用前显示 diff 和影响范围
- 应用后可以回滚

**配置示例：**
```json
{
  "experience_hub": {
    "auto_apply_improvements": false,
    "auto_download_improvements": true,
    "require_confirmation": true
  }
}
```

### 6.2 硬规则 4：中心分发的内容必须可签名、可追溯、可回滚

**可签名：**
- 所有分发包使用 Ed25519 签名
- 签名密钥由中心管理
- 用户可以验证签名有效性

**可追溯：**
- 每个改进建议包含：
  - 来源实例数量（匿名）
  - 审核员 ID（匿名）
  - 创建时间
  - 版本号
- 用户可以查看改进建议的来源和审核历史

**可回滚：**
- 应用改进后，自动创建回滚点
- 用户可以一键回滚到应用前状态
- 回滚点包含：
  - 原始文件备份
  - 应用的改进 ID
  - 应用时间
  - 回滚命令

### 6.3 分发原则

**原则 1：最小化原则**
- 只分发经过验证的改进
- 不分发实验性功能
- 不分发未经审核的规则

**原则 2：透明原则**
- 改进建议包含完整说明
- 用户可以查看 diff
- 用户可以查看验证结果

**原则 3：可控原则**
- 用户可以选择性应用
- 用户可以设置过滤规则
- 用户可以禁用特定类型的改进

**原则 4：安全原则**
- 所有改进经过安全扫描
- 不分发高风险改进（文件删除、权限提升等）
- 不分发未签名的内容

### 6.4 禁止项

**禁止分发：**
1. 未经审核的代码
2. 包含恶意内容的改进
3. 包含隐私泄露的数据
4. 未签名的分发包
5. 高风险操作（文件删除、系统修改等）
6. 实验性功能（未经充分验证）
7. 第三方依赖（未经安全审计）

**禁止自动应用：**
1. 任何代码修改
2. 任何配置修改
3. 任何文件操作
4. 任何网络请求
5. 任何权限提升

**禁止收集：**
1. 原始日志
2. 用户输入内容
3. 文件内容
4. API 密钥
5. 个人信息
6. 未经脱敏的执行记录

---

## 7. Phase 0 实施路径

### 7.1 Phase 0 验收标准

**Phase 0 成功标准（4 条）：**

1. **本地能生成一份 stats-only 经验包**
   - 包含必需字段（version, instance_id, generated_at, taijios_version, privacy_level, period, stats）
   - 格式符合 Schema 定义
   - 文件大小 < 10MB

2. **经验包能通过格式验证和脱敏检查**
   - JSON schema 验证通过
   - 脱敏扫描通过（无密钥、路径、原始日志）
   - 隐私级别正确（默认 stats-only）

3. **中心侧能对经验包做接收/拒收判定**
   - 实现拒收规则（5 类，见 5.2.1）
   - 能返回明确的拒收原因
   - 能记录拒收日志

4. **中心侧能输出一份人工审核后的改进建议包**
   - 包含必需字段（package_id, source_batch_id, generated_at, signature, rollback_id, compatibility_version）
   - 签名验证通过
   - 但不自动分发、不自动应用

**验收方式：**
- 手动生成经验包
- 手动提交到中心（HTTP POST）
- 手动审核
- 手动生成改进建议包
- 手动验证签名

**不包含：**
- 自动上传
- 自动聚合
- 自动分发
- 自动应用

### 7.2 当前阶段目标

### 7.2 当前阶段目标

**Phase 0: 手动经验包**

**目标：**
- 定义协议和格式
- 实现本地经验包生成
- 实现脱敏和验证
- 实现中心侧接收/拒收判定
- 实现人工审核流程
- 不实现自动上传和分发

**交付物：**
1. 本地经验包生成工具（CLI）
2. 脱敏验证工具
3. 经验包查看器
4. 中心侧接收/拒收判定工具
5. 协议文档（本文档）

### 7.3 不做的事

**Phase 0 不包含：**
- 中心服务实现
- 自动上传功能
- 自动分发功能
- 自动应用功能
- 聚合分析功能

**原因：**
- 需要先验证协议设计
- 需要先积累足够的经验包样本
- 需要先建立用户信任
- 需要先解决隐私和安全问题

### 7.4 下一步

**Phase 1: 中心服务（未来）**
- 实现中心接收和验证
- 实现人工审核流程
- 实现改进建议生成

**Phase 2: 自动化（未来）**
- 实现自动上传（opt-in）
- 实现自动下载改进建议
- 实现半自动应用（需确认）

**Phase 3: 生态化（未来）**
- 实现多中心支持
- 实现去中心化验证
- 实现社区治理

---

## 8. 协议审查清单

### 8.1 Schema 最小化审查

**Phase 0 必需字段（7 个）：**
- ✅ `version` - 协议版本
- ✅ `instance_id` - 实例标识
- ✅ `generated_at` - 生成时间
- ✅ `taijios_version` - 系统版本
- ✅ `privacy_level` - 隐私级别
- ✅ `period` - 统计时间范围
- ✅ `stats` - 统计数据

**Phase 0 可选字段（2 个）：**
- ✅ `lessons_summary` - lesson 候选摘要
- ✅ `rules_summary` - rule 候选摘要

**Phase 0 不包含：**
- ❌ `experiences` - 原始经验列表
- ❌ `metadata` - 详细元数据
- ❌ 任何原始日志或执行记录

**结论：** Schema 已收紧到最小集。

### 8.2 脱敏级别审查

**Phase 0 固定 3 档：**
- ✅ `stats-only` - 只传统计数据（默认）
- ✅ `summary-only` - stats + lesson/rule 候选摘要
- ✅ `review-required` - stats + summaries + 需人工审核的详细信息

**Phase 0 不支持：**
- ❌ `experiences` - 原始经验记录
- ❌ `full` - 所有数据

**结论：** 脱敏级别已固化，默认值明确。

### 8.3 中心侧拒收规则审查

**5 类拒收规则：**
- ✅ 含密钥/令牌
- ✅ 含路径/账号/主机名
- ✅ 含原始长日志
- ✅ 含不可解释的可执行内容
- ✅ 含来源不明的规则包

**拒收响应格式：**
- ✅ 明确的拒收原因
- ✅ 详细的错误位置
- ✅ 时间戳

**结论：** 拒收规则已写硬。

### 8.4 分发包身份信息审查

**Phase 0 必需字段（6 个）：**
- ✅ `package_id` - 分发包唯一标识
- ✅ `source_batch_id` - 来源批次标识
- ✅ `generated_at` - 生成时间
- ✅ `signature` - Ed25519 签名
- ✅ `rollback_id` - 回滚点标识
- ✅ `compatibility_version` - 兼容版本

**结论：** 分发包身份信息已固定。

### 8.5 Phase 0 成功标准审查

**4 条验收标准：**
- ✅ 本地能生成一份 stats-only 经验包
- ✅ 经验包能通过格式验证和脱敏检查
- ✅ 中心侧能对经验包做接收/拒收判定
- ✅ 中心侧能输出一份人工审核后的改进建议包

**结论：** Phase 0 成功标准已明确。

---

## 9. 安全与隐私保障

### 9.1 隐私保护

**数据最小化：**
- 只收集必要的统计数据
- 不收集原始日志
- 不收集用户内容

**匿名化：**
- 实例 ID 使用 UUID（不可追溯到用户）
- 不收集用户名、邮箱、IP 地址
- 不收集硬件序列号、MAC 地址

**用户控制：**
- 用户可以随时禁用上传
- 用户可以删除已上传的数据
- 用户可以查看已上传的内容

### 9.2 安全保障

**传输安全：**
- 使用 HTTPS 传输
- 使用 TLS 1.3
- 验证服务器证书

**存储安全：**
- 中心数据加密存储
- 定期备份
- 访问控制

**代码安全：**
- 所有改进经过安全扫描
- 不分发未签名的代码
- 不自动执行未验证的代码

### 9.3 合规性

**GDPR 合规：**
- 用户可以请求删除数据
- 用户可以导出已上传的数据
- 明确的隐私声明

**透明度：**
- 公开协议文档
- 公开数据处理流程
- 公开审核标准

---

## 10. 开放问题

### 10.1 待解决的问题

1. **中心服务架构**
   - 单中心 vs 多中心
   - 中心化 vs 去中心化
   - 服务可用性保障

2. **审核流程**
   - 人工审核 vs 自动审核
   - 审核标准细化
   - 审核员培训

3. **激励机制**
   - 如何激励用户上传经验
   - 如何激励审核员参与
   - 如何平衡质量和数量

4. **版本兼容性**
   - 如何处理不同版本的太极OS
   - 如何处理协议升级
   - 如何处理向后兼容

5. **冲突解决**
   - 如何处理矛盾的改进建议
   - 如何处理不同实例的差异
   - 如何处理用户自定义规则

### 10.2 需要进一步讨论的方向

1. **去中心化可能性**
   - 是否需要去中心化
   - 如何实现去中心化验证
   - 如何保证去中心化的安全性

2. **社区治理**
   - 如何让社区参与审核
   - 如何建立信任机制
   - 如何处理恶意行为

3. **商业模式**
   - 中心服务如何维持
   - 是否需要付费功能
   - 如何平衡开源和商业

---

## 11. 总结

### 11.1 核心原则

1. **隐私优先** - 默认不上传，默认只传统计数据
2. **用户控制** - 用户可以控制所有数据和行为
3. **安全第一** - 所有内容经过验证和签名
4. **透明可信** - 所有流程公开透明
5. **渐进演进** - 从手动到自动，从中心到去中心

### 11.2 当前定位

Experience Hub 是一个 **P2/P3 级的生态方向**，不是当前主线。

**Phase 0 目标：** 定义协议，实现本地经验包生成，不做自动化。

**下一步：** 积累经验包样本，验证协议设计，建立用户信任。

### 11.3 成功标准

**Phase 0 成功标准：**
1. 协议文档完成并经过审查
2. 本地经验包生成工具可用
3. 至少 10 个实例生成经验包
4. 经验包格式稳定，无重大修改

**长期成功标准：**
1. 至少 100 个实例参与经验共享
2. 至少 50 条改进建议被应用
3. 用户满意度 > 80%
4. 无重大隐私或安全事故

---

**文档版本：** v0.2  
**最后更新：** 2026-03-11  
**维护者：** 小九 + 珊瑚海  
**状态：** design-only / draft / review-pending  
**实施状态：** not started

---

## 附录：协议审查总结

### A.1 Schema 最小化 ✅
- Phase 0 必需字段：7 个
- Phase 0 可选字段：2 个
- 不包含原始日志、执行记录、用户数据

### A.2 脱敏级别固化 ✅
- 固定 3 档：stats-only / summary-only / review-required
- 默认值：stats-only
- Phase 0 不支持 experiences / full

### A.3 中心侧拒收规则 ✅
- 5 类拒收规则已写硬
- 拒收响应格式已定义
- 拒收原因可追溯

### A.4 分发包身份信息 ✅
- 6 个必需字段已固定
- 签名验证机制已定义
- 可追溯、可回滚

### A.5 Phase 0 成功标准 ✅
- 4 条验收标准已明确
- 验收方式已定义
- 不包含自动化功能

**协议审查结论：** 协议设计已收紧到 Phase 0 最小集，边界清晰，可进入实施准备阶段。
