# 太极OS (AIOS) - 核心依赖

# === 必需 ===
requests>=2.31.0           # HTTP 请求（Agent 通信、API 调用）
Flask>=3.0.0               # Dashboard 后端
flask-cors>=4.0.0          # CORS 支持

# === 推荐（功能增强）===
# sentence-transformers    # Memory Server embedding 模型
# aiosqlite>=0.20.0        # 异步 SQLite（events DB）

# === 可选（特定功能）===
# Flask-Login>=0.6.3       # Dashboard 用户认证（当前未启用）
# numpy                    # 数值计算（embedding 相关）
