# AIOS Production Cron Jobs Update Commands
# Generated: 2026-03-03
# Environment: Production

## Current Cron Jobs (to be updated)

### 1. Heartbeat (Every 30 minutes)
```bash
cron add \
  --schedule "*/30 * * * *" \
  --session isolated \
  --payload.kind agentTurn \
  --payload.message "Read HEARTBEAT.md if it exists (workspace context). Follow it strictly. Do not infer or repeat old tasks from prior chats. If nothing needs attention, reply HEARTBEAT_OK." \
  --delivery.mode announce \
  --name "AIOS Heartbeat (Production)"
```

### 2. Learning Agents (Daily 08:50 - off-peak)
```bash
cron add \
  --schedule "50 8 * * *" \
  --session isolated \
  --payload.kind agentTurn \
  --payload.message "Run all learning agents: GitHub research, architecture analysis, code review, documentation updates, and idea generation. Focus on AIOS improvements." \
  --delivery.mode announce \
  --name "AIOS Learning (Production)"
```

### 3. Health Check (Every 6 hours)
```bash
cron add \
  --schedule "0 */6 * * *" \
  --session isolated \
  --payload.kind agentTurn \
  --payload.message "Run AIOS health check: system metrics, agent status, error rates, and cost tracking. Alert if health score < 60." \
  --delivery.mode announce \
  --name "AIOS Health Check (Production)"
```

### 4. Cleanup (Daily 02:00 - low-traffic time)
```bash
cron add \
  --schedule "0 2 * * *" \
  --session isolated \
  --payload.kind agentTurn \
  --payload.message "Run AIOS cleanup: remove old logs (>7 days), archive completed tasks, clean temp files, and optimize database." \
  --delivery.mode announce \
  --name "AIOS Cleanup (Production)"
```

### 5. Backup (Daily 03:00)
```bash
cron add \
  --schedule "0 3 * * *" \
  --session isolated \
  --payload.kind agentTurn \
  --payload.message "Run AIOS backup: database, configuration, and critical data. Retain last 30 days." \
  --delivery.mode announce \
  --name "AIOS Backup (Production)"
```

### 6. Daily Reports (Daily 09:00 - after learning)
```bash
cron add \
  --schedule "0 9 * * *" \
  --session isolated \
  --payload.kind agentTurn \
  --payload.message "Generate AIOS daily report: task completion, agent performance, cost summary, and improvement suggestions." \
  --delivery.mode announce \
  --name "AIOS Daily Report (Production)"
```

---

## Execution Steps

### Step 1: List Current Cron Jobs
```bash
cron list
```

### Step 2: Remove Old Jobs (if any)
```bash
# Find job IDs from step 1, then remove:
cron remove --jobId <job-id>
```

### Step 3: Add New Jobs
Execute the 6 commands above in order.

### Step 4: Verify
```bash
cron list
```

Expected output: 6 jobs with correct schedules.

---

## Schedule Summary

| Job | Schedule | Time (GMT+8) | Frequency |
|-----|----------|--------------|-----------|
| Heartbeat | `*/30 * * * *` | Every 30 min | 48x/day |
| Learning | `50 8 * * *` | 08:50 | Daily |
| Health Check | `0 */6 * * *` | 00:00, 06:00, 12:00, 18:00 | 4x/day |
| Cleanup | `0 2 * * *` | 02:00 | Daily |
| Backup | `0 3 * * *` | 03:00 | Daily |
| Reports | `0 9 * * *` | 09:00 | Daily |

**Total daily executions:** ~54 (48 heartbeats + 6 scheduled jobs)

---

## Notes

1. **Off-peak scheduling:** Learning (08:50) runs before Reports (09:00) to avoid conflicts.
2. **Low-traffic cleanup:** Cleanup (02:00) and Backup (03:00) run during low-traffic hours.
3. **Delivery mode:** All jobs use `announce` mode (notify on completion).
4. **Session isolation:** All jobs run in isolated sessions to avoid polluting main session history.

---

## Rollback (if needed)

If you need to revert to old schedules:
1. Remove all new jobs: `cron list` → `cron remove --jobId <id>`
2. Re-add old jobs from backup

---

## Environment Variables Required

Before running cron jobs, ensure these are set:

```bash
# OpenClaw API Key
export OPENCLAW_API_KEY="your-api-key"

# Telegram Chat ID (for notifications)
export TELEGRAM_CHAT_ID="your-chat-id"
```

Check with:
```bash
echo $OPENCLAW_API_KEY
echo $TELEGRAM_CHAT_ID
```

---

**Generated by:** AIOS Config Manager  
**Date:** 2026-03-03  
**Config:** prod.yaml v1.0
